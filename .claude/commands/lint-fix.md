Run `ruff check . --fix` to auto-fix all fixable lint issues, then run `ruff format .` to format.

If any remaining issues cannot be auto-fixed, read the affected files and fix them manually.

After all fixes are applied, run `ruff check .` one final time to confirm zero issues remain.
